info:
  name: Add Note
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/notes
  body:
    type: json
    data: |-
      {
        "user_id": 2,
        "country_id": 10,
        "content": "Twas a good time"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

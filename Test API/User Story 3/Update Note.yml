info:
  name: Update Note
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8080/notes/1/Was it though
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

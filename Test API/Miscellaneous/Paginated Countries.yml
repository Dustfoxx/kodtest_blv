info:
  name: Paginated Countries
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/countries/2/15
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

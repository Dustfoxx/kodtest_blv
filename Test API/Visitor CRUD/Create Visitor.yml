info:
  name: Create Visitor
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/visitors
  body:
    type: json
    data: '{"name": "Tim Drake"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

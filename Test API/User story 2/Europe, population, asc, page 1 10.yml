info:
  name: Europe, population, asc, page 1 10
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/countries
  body:
    type: json
    data: |-
      {
        "page": "1",
        "items_per_page": 10,
        "region": "Europe",
        "population": true,
        "asc": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

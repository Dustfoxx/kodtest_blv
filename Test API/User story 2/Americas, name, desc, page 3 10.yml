info:
  name: Americas, name, desc, page 3 10
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/countries
  body:
    type: json
    data: |-
      {
        "page": "3",
        "items_per_page": 10,
        "region": "Americas",
        "population": false,
        "asc": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
